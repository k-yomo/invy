
mutation muteFriend($userId: UUID!) {
    muteUser(userId: $userId) {
        mutedUserId
    }
}

mutation unmuteFriend($userId: UUID!) {
    unmuteUser(userId:  $userId) {
        unmutedUserId
    }
}
