
mutation registerInvitationAwaiting($input: RegisterInvitationAwaitingInput!) {
    registerInvitationAwaiting(input: $input) {
        invitationAwaiting {
            id
            userId
            startsAt
            endsAt
            comment
        }
    }
}
