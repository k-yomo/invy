query viewer {
    viewer {
        id
        screenId
        nickname
        avatarUrl
    }
}

mutation signUp($input: SignUpInput!) {
    signUp(input: $input) {
        viewer {
            id
            screenId
            nickname
            avatarUrl
        }
    }
}
