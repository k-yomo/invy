query friendGroupDetailScreenViewer($friendGroupId: UUID!) {
    viewer {
        friendGroup(friendGroupId: $friendGroupId) {
            ...friendGroupEditScreenFragment,
            id,
            name,
            totalCount,
            friendUsers {
                ...friendListItemFragment
                ...userProfileModalFragment
            }
        }
    }
}
