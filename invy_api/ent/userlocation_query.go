// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/k-yomo/invy/invy_api/ent/predicate"
	"github.com/k-yomo/invy/invy_api/ent/user"
	"github.com/k-yomo/invy/invy_api/ent/userlocation"
)

// UserLocationQuery is the builder for querying UserLocation entities.
type UserLocationQuery struct {
	config
	ctx        *QueryContext
	order      []userlocation.OrderOption
	inters     []Interceptor
	predicates []predicate.UserLocation
	withUser   *UserQuery
	modifiers  []func(*sql.Selector)
	loadTotal  []func(context.Context, []*UserLocation) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserLocationQuery builder.
func (ulq *UserLocationQuery) Where(ps ...predicate.UserLocation) *UserLocationQuery {
	ulq.predicates = append(ulq.predicates, ps...)
	return ulq
}

// Limit the number of records to be returned by this query.
func (ulq *UserLocationQuery) Limit(limit int) *UserLocationQuery {
	ulq.ctx.Limit = &limit
	return ulq
}

// Offset to start from.
func (ulq *UserLocationQuery) Offset(offset int) *UserLocationQuery {
	ulq.ctx.Offset = &offset
	return ulq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ulq *UserLocationQuery) Unique(unique bool) *UserLocationQuery {
	ulq.ctx.Unique = &unique
	return ulq
}

// Order specifies how the records should be ordered.
func (ulq *UserLocationQuery) Order(o ...userlocation.OrderOption) *UserLocationQuery {
	ulq.order = append(ulq.order, o...)
	return ulq
}

// QueryUser chains the current query on the "user" edge.
func (ulq *UserLocationQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: ulq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ulq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ulq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userlocation.Table, userlocation.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, userlocation.UserTable, userlocation.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(ulq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserLocation entity from the query.
// Returns a *NotFoundError when no UserLocation was found.
func (ulq *UserLocationQuery) First(ctx context.Context) (*UserLocation, error) {
	nodes, err := ulq.Limit(1).All(setContextOp(ctx, ulq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userlocation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ulq *UserLocationQuery) FirstX(ctx context.Context) *UserLocation {
	node, err := ulq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserLocation ID from the query.
// Returns a *NotFoundError when no UserLocation ID was found.
func (ulq *UserLocationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ulq.Limit(1).IDs(setContextOp(ctx, ulq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userlocation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ulq *UserLocationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ulq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserLocation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserLocation entity is found.
// Returns a *NotFoundError when no UserLocation entities are found.
func (ulq *UserLocationQuery) Only(ctx context.Context) (*UserLocation, error) {
	nodes, err := ulq.Limit(2).All(setContextOp(ctx, ulq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userlocation.Label}
	default:
		return nil, &NotSingularError{userlocation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ulq *UserLocationQuery) OnlyX(ctx context.Context) *UserLocation {
	node, err := ulq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserLocation ID in the query.
// Returns a *NotSingularError when more than one UserLocation ID is found.
// Returns a *NotFoundError when no entities are found.
func (ulq *UserLocationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ulq.Limit(2).IDs(setContextOp(ctx, ulq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userlocation.Label}
	default:
		err = &NotSingularError{userlocation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ulq *UserLocationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ulq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserLocations.
func (ulq *UserLocationQuery) All(ctx context.Context) ([]*UserLocation, error) {
	ctx = setContextOp(ctx, ulq.ctx, "All")
	if err := ulq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserLocation, *UserLocationQuery]()
	return withInterceptors[[]*UserLocation](ctx, ulq, qr, ulq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ulq *UserLocationQuery) AllX(ctx context.Context) []*UserLocation {
	nodes, err := ulq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserLocation IDs.
func (ulq *UserLocationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if ulq.ctx.Unique == nil && ulq.path != nil {
		ulq.Unique(true)
	}
	ctx = setContextOp(ctx, ulq.ctx, "IDs")
	if err = ulq.Select(userlocation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ulq *UserLocationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ulq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ulq *UserLocationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ulq.ctx, "Count")
	if err := ulq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ulq, querierCount[*UserLocationQuery](), ulq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ulq *UserLocationQuery) CountX(ctx context.Context) int {
	count, err := ulq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ulq *UserLocationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ulq.ctx, "Exist")
	switch _, err := ulq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ulq *UserLocationQuery) ExistX(ctx context.Context) bool {
	exist, err := ulq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserLocationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ulq *UserLocationQuery) Clone() *UserLocationQuery {
	if ulq == nil {
		return nil
	}
	return &UserLocationQuery{
		config:     ulq.config,
		ctx:        ulq.ctx.Clone(),
		order:      append([]userlocation.OrderOption{}, ulq.order...),
		inters:     append([]Interceptor{}, ulq.inters...),
		predicates: append([]predicate.UserLocation{}, ulq.predicates...),
		withUser:   ulq.withUser.Clone(),
		// clone intermediate query.
		sql:  ulq.sql.Clone(),
		path: ulq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (ulq *UserLocationQuery) WithUser(opts ...func(*UserQuery)) *UserLocationQuery {
	query := (&UserClient{config: ulq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ulq.withUser = query
	return ulq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserLocation.Query().
//		GroupBy(userlocation.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ulq *UserLocationQuery) GroupBy(field string, fields ...string) *UserLocationGroupBy {
	ulq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserLocationGroupBy{build: ulq}
	grbuild.flds = &ulq.ctx.Fields
	grbuild.label = userlocation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//	}
//
//	client.UserLocation.Query().
//		Select(userlocation.FieldUserID).
//		Scan(ctx, &v)
func (ulq *UserLocationQuery) Select(fields ...string) *UserLocationSelect {
	ulq.ctx.Fields = append(ulq.ctx.Fields, fields...)
	sbuild := &UserLocationSelect{UserLocationQuery: ulq}
	sbuild.label = userlocation.Label
	sbuild.flds, sbuild.scan = &ulq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserLocationSelect configured with the given aggregations.
func (ulq *UserLocationQuery) Aggregate(fns ...AggregateFunc) *UserLocationSelect {
	return ulq.Select().Aggregate(fns...)
}

func (ulq *UserLocationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ulq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ulq); err != nil {
				return err
			}
		}
	}
	for _, f := range ulq.ctx.Fields {
		if !userlocation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ulq.path != nil {
		prev, err := ulq.path(ctx)
		if err != nil {
			return err
		}
		ulq.sql = prev
	}
	return nil
}

func (ulq *UserLocationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserLocation, error) {
	var (
		nodes       = []*UserLocation{}
		_spec       = ulq.querySpec()
		loadedTypes = [1]bool{
			ulq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserLocation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserLocation{config: ulq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ulq.modifiers) > 0 {
		_spec.Modifiers = ulq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ulq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ulq.withUser; query != nil {
		if err := ulq.loadUser(ctx, query, nodes, nil,
			func(n *UserLocation, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	for i := range ulq.loadTotal {
		if err := ulq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ulq *UserLocationQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*UserLocation, init func(*UserLocation), assign func(*UserLocation, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*UserLocation)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ulq *UserLocationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ulq.querySpec()
	if len(ulq.modifiers) > 0 {
		_spec.Modifiers = ulq.modifiers
	}
	_spec.Node.Columns = ulq.ctx.Fields
	if len(ulq.ctx.Fields) > 0 {
		_spec.Unique = ulq.ctx.Unique != nil && *ulq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ulq.driver, _spec)
}

func (ulq *UserLocationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userlocation.Table, userlocation.Columns, sqlgraph.NewFieldSpec(userlocation.FieldID, field.TypeUUID))
	_spec.From = ulq.sql
	if unique := ulq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ulq.path != nil {
		_spec.Unique = true
	}
	if fields := ulq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userlocation.FieldID)
		for i := range fields {
			if fields[i] != userlocation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ulq.withUser != nil {
			_spec.Node.AddColumnOnce(userlocation.FieldUserID)
		}
	}
	if ps := ulq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ulq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ulq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ulq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ulq *UserLocationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ulq.driver.Dialect())
	t1 := builder.Table(userlocation.Table)
	columns := ulq.ctx.Fields
	if len(columns) == 0 {
		columns = userlocation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ulq.sql != nil {
		selector = ulq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ulq.ctx.Unique != nil && *ulq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ulq.predicates {
		p(selector)
	}
	for _, p := range ulq.order {
		p(selector)
	}
	if offset := ulq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ulq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserLocationGroupBy is the group-by builder for UserLocation entities.
type UserLocationGroupBy struct {
	selector
	build *UserLocationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ulgb *UserLocationGroupBy) Aggregate(fns ...AggregateFunc) *UserLocationGroupBy {
	ulgb.fns = append(ulgb.fns, fns...)
	return ulgb
}

// Scan applies the selector query and scans the result into the given value.
func (ulgb *UserLocationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ulgb.build.ctx, "GroupBy")
	if err := ulgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserLocationQuery, *UserLocationGroupBy](ctx, ulgb.build, ulgb, ulgb.build.inters, v)
}

func (ulgb *UserLocationGroupBy) sqlScan(ctx context.Context, root *UserLocationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ulgb.fns))
	for _, fn := range ulgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ulgb.flds)+len(ulgb.fns))
		for _, f := range *ulgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ulgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ulgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserLocationSelect is the builder for selecting fields of UserLocation entities.
type UserLocationSelect struct {
	*UserLocationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (uls *UserLocationSelect) Aggregate(fns ...AggregateFunc) *UserLocationSelect {
	uls.fns = append(uls.fns, fns...)
	return uls
}

// Scan applies the selector query and scans the result into the given value.
func (uls *UserLocationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uls.ctx, "Select")
	if err := uls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserLocationQuery, *UserLocationSelect](ctx, uls.UserLocationQuery, uls, uls.inters, v)
}

func (uls *UserLocationSelect) sqlScan(ctx context.Context, root *UserLocationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(uls.fns))
	for _, fn := range uls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*uls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
